USE `aluma_db`
GO

/* SQLINES DEMO *** le [dbo].[documents]    Script Date: 12/31/2020 08:44:20 ******/
SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE TABLE `dbo`.[documents](
	`serial` int AUTO_INCREMENT NOT NULL,
	`client_serial` int NULL,
	`comment` nvarchar(150) NULL,
	`type_doc` int NULL,
	`document_occupation` int NULL,
	`date_of_document` datetime NULL,
	`file_name` nvarchar(100) NULL,
	`no_police` nvarchar(50) NULL,
	`conversation_serial` int NULL,
	`user_serial` int NULL,
 CONSTRAINT `PK_documents` PRIMARY KEY 
(
	`serial` ASC
) 
) ON [PRIMARY]

GO

ALTER TABLE `dbo`.`documents`  WITH CHECK ADD  CONSTRAINT `FK_documents_client` FOREIGN KEY(`client_serial`)
REFERENCES [dbo].[clients] (`serial`)
GO

ALTER TABLE `dbo`.`documents` CHECK CONSTRAINT `FK_documents_client`
GO


